import { COLORS, FONT_SIZE } from "../../constants/theme";

export const styles = {
    container: {
        backgroundColor: COLORS.white,
        flex: 1,
        paddingTop: 30,
    },
    text: {
        fontSize: FONT_SIZE.md,
        color: COLORS.gray1,
        marginTop: 15,
        marginLeft: 10,
    }
};